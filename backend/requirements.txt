# LangChain 和 LangGraph
langchain>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-socketio>=5.11.0
python-multipart>=0.0.9

# 数据库
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
pgvector>=0.3.0

# AI 服务
openai>=1.50.0
anthropic>=0.34.0

# 知识图谱 / Graphiti 记忆层
graphiti-core[neo4j]>=0.3.0
neo4j>=5.26.0

# 工具库
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# 数据分析
scipy>=1.11.0
numpy>=1.24.0

# 文件处理
pypdf>=5.0.0
pillow>=10.4.0
opencv-python>=4.10.0

# 语音处理 (Speech-Processing 模块)
websocket-client>=1.8.0  # 阿里云 NLS WebSocket 客户端

# 其他工具
httpx>=0.27.0
aiofiles>=24.1.0
redis>=5.0.0
celery>=5.4.0

# 开发工具
pytest>=8.3.0
pytest-asyncio>=0.24.0
black>=24.8.0
flake8>=7.1.0
mypy>=1.11.0

# 注意事项:
# 1. Speech-Processing 模块需要系统安装 ffmpeg 才能进行音频格式转换
#    Windows: choco install ffmpeg 或从 https://ffmpeg.org/ 下载
#    Mac: brew install ffmpeg
#    Linux: sudo apt-get install ffmpeg
#
# 2. Multimodal-Understanding 模块使用 OpenAI 兼容接口，需要配置:
#    DASHSCOPE_API_KEY - 阿里云 DashScope API Key
#    DASHSCOPE_BASE_URL - API 地址 (默认: https://dashscope.aliyuncs.com/compatible-mode/v1)
#    DASHSCOPE_MODEL - 模型名称 (默认: qwen3-vl-plus)
#
# 3. SQLite 模块无额外系统依赖，开箱即用
