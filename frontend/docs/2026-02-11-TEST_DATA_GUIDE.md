# 测试数据注入指南

## 快速开始

### 方法一：使用网页工具（推荐）

1. 启动开发服务器：
   ```bash
   cd frontend
   npm run dev
   ```

2. 在浏览器中访问：
   ```
   http://localhost:3000/seed-data.html
   ```

3. 点击"🚀 初始化所有测试数据"按钮

4. 返回主应用，访问"兴趣雷达图"页面查看效果

### 方法二：使用浏览器控制台

1. 启动应用并打开浏览器控制台（F12）

2. 在控制台中输入以下命令：
   ```javascript
   initTestData()
   ```

3. 刷新页面，访问"兴趣雷达图"页面

## 可用命令

在浏览器控制台中可以使用以下命令：

### `initTestData()`
清空所有数据并初始化完整的测试数据集（推荐使用）

```javascript
initTestData()
```

### `clearData()`
清空 localStorage 中的所有数据

```javascript
clearData()
```

### `seedProfile()`
仅创建测试用的孩子档案

```javascript
seedProfile()
```

### `seedBehaviors()`
仅生成行为数据（需要先有孩子档案）

```javascript
seedBehaviors()
```

## 测试数据说明

### 孩子档案
- 姓名：小明
- 性别：男
- 出生日期：2020-03-15
- 诊断：轻度自闭症谱系障碍，语言发展迟缓

### 行为数据特点

#### 时间跨度
- 30 天（从今天往前推 30 天）
- 每天 2-3 条行为记录
- 总计约 60+ 条记录

#### 发展阶段

**第 1-5 天：初期探索**
- 主要维度：视觉（Visual）、触觉（Tactile）
- 特点：对视觉刺激和触觉体验表现出兴趣
- 强度：中等偏高（0.6-0.8）

**第 6-10 天：建构兴趣萌芽**
- 主要维度：建构（Construction）、听觉（Auditory）
- 特点：开始尝试搭建积木，对音乐有反应
- 强度：中等（0.5-0.6）

**第 11-15 天：秩序感建立**
- 主要维度：建构（Construction）、秩序（Order）
- 特点：建构能力提升，开始按规则分类
- 强度：较高（0.6-0.8）

**第 16-20 天：认知和社交启蒙**
- 主要维度：认知（Cognitive）、社交（Social）
- 特点：问题解决能力提升，开始主动寻求互动
- 强度：中等偏高（0.5-0.7）

**第 21-25 天：运动和社交增强**
- 主要维度：运动（Motor）、社交（Social）
- 特点：大运动能力提升，主动参与群体活动
- 强度：高（0.7-0.8）

**第 26-30 天：全面发展**
- 主要维度：所有维度均衡发展
- 特点：社交沟通能力显著提升，建构能力达到新高度
- 强度：很高（0.7-0.9）
- 包含负面强度数据（听觉敏感：-0.6）

#### 数据来源分布
- CHAT：约 40 条（日常观察记录）
- GAME：约 20 条（游戏中的行为）
- REPORT：0 条

#### 兴趣维度覆盖
所有 8 个维度都有数据：
- Visual（视觉）
- Auditory（听觉）
- Tactile（触觉）
- Motor（运动）
- Construction（建构）
- Order（秩序）
- Cognitive（认知）
- Social（社交）

## 预期效果

### 雷达图时间轴

1. **初期（第 1-5 天）**
   - 视觉和触觉维度突出
   - 其他维度较低

2. **中期（第 6-15 天）**
   - 建构维度快速增长
   - 秩序维度开始显现
   - 视觉维度持续增长

3. **后期（第 16-30 天）**
   - 社交和认知维度显著提升
   - 运动维度快速增长
   - 所有维度趋于均衡发展

### 关联度 vs 强度

- **关联度**：持续累积增长，反映行为记录的数量和关联程度
- **强度**：有正有负，反映孩子的真实喜好
  - 正值最高：建构、社交、运动
  - 负值：听觉（对突然噪音的抗拒）

## 测试场景

### 1. 时间轴拖动
- 拖动滑块从第 1 天到第 30 天
- 观察雷达图的形状变化
- 查看数据摘要的更新

### 2. 自动播放
- 点击"播放"按钮
- 观察雷达图的动态演变
- 验证动画效果是否流畅

### 3. 显示模式切换
- 切换"关联度"、"强度"、"两者"模式
- 验证数据显示是否正确
- 检查图例和颜色

### 4. 数据摘要
- 验证每个时间点的摘要是否准确
- 检查"关联度最高"、"最喜欢"、"最抗拒"的维度

### 5. 交互功能
- 鼠标悬停查看详细数据
- 验证 Tooltip 显示
- 检查累计值计算

## 故障排除

### 问题：控制台命令不可用
**解决方案**：
1. 确保已启动开发服务器
2. 刷新页面
3. 检查控制台是否有错误信息

### 问题：数据没有显示
**解决方案**：
1. 打开浏览器控制台，检查 localStorage
2. 运行 `localStorage.getItem('asd_floortime_behaviors')`
3. 如果为空，重新运行 `initTestData()`

### 问题：雷达图显示异常
**解决方案**：
1. 清空数据：`clearData()`
2. 重新初始化：`initTestData()`
3. 刷新页面

### 问题：时间轴没有数据点
**解决方案**：
1. 检查行为数据是否有 timestamp 字段
2. 验证日期格式是否正确（ISO 8601）
3. 重新生成数据

## 数据持久化

- 数据存储在浏览器的 localStorage 中
- 关闭浏览器后数据仍然保留
- 清空浏览器缓存会丢失数据
- 可以随时使用 `clearData()` 清空数据

## 开发建议

### 添加新的测试场景

编辑 `frontend/src/utils/seedTestData.ts`，在 `seedBehaviorData()` 函数中添加新的行为记录：

```typescript
behaviors.push(
  generateBehavior(
    date,
    '行为描述',
    [
      { 
        dimension: 'Visual', 
        weight: 0.8, 
        intensity: 0.7, 
        reasoning: '原因说明' 
      }
    ],
    'CHAT'
  )
);
```

### 调整时间跨度

修改循环范围来改变时间跨度：

```typescript
// 从 30 天改为 60 天
for (let i = 59; i >= 0; i--) {
  // ...
}
```

### 自定义孩子档案

修改 `seedChildProfile()` 函数中的档案信息：

```typescript
const profile: ChildProfile = {
  name: '你的名字',
  gender: '性别',
  birthDate: 'YYYY-MM-DD',
  // ...
};
```

## 相关文件

- `frontend/src/utils/seedTestData.ts` - 测试数据生成脚本
- `frontend/seed-data.html` - 网页工具界面
- `frontend/src/services/radarChartService.ts` - 雷达图数据服务
- `frontend/src/components/RadarChartPage.tsx` - 雷达图页面组件

## 更新日志

### 2026-02-11
- ✅ 创建测试数据注入脚本
- ✅ 添加网页工具界面
- ✅ 生成 30 天的行为数据
- ✅ 模拟 5 个发展阶段
- ✅ 包含正负强度数据
- ✅ 支持浏览器控制台命令
