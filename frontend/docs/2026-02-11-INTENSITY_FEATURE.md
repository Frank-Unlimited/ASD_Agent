# 关联度与强度分离功能说明

## 概述
本次更新将行为分析中的"关联度"和"强度"分离为两个独立的指标，以更准确地描述孩子的行为特征。

## 核心概念

### 1. 关联度 (weight)
- **范围**: 0.1 - 1.0（只能是正值）
- **含义**: 行为与兴趣维度的关联程度
- **示例**:
  - 1.0 = 强关联："玩积木" 与 Construction（建构）
  - 0.7 = 中等关联："玩积木" 与 Visual（视觉）
  - 0.4 = 弱关联："玩积木" 与 Social（社交）

### 2. 强度 (intensity)
- **范围**: -1.0 到 1.0
- **含义**: 孩子对该维度的喜欢/讨厌程度
- **示例**:
  - +1.0 = 非常喜欢："兴奋地玩积木，不愿停止"
  - +0.5 = 比较喜欢："专注地玩积木"
  - 0.0 = 中性："被动地接受"
  - -0.5 = 比较抗拒："推开积木"
  - -1.0 = 非常讨厌："哭闹着拒绝"

## 数据结构变化

### InterestMatch 类型
```typescript
export interface InterestMatch {
  dimension: InterestDimensionType;
  weight: number;      // 0.1-1.0 关联度
  intensity: number;   // -1.0到1.0 强度
  reasoning: string;
}
```

## UI 显示

### 行为详情弹窗
1. **关联度显示**
   - 单向进度条（0-100%）
   - 颜色对应维度配色

2. **强度显示**
   - 双向进度条（中心线为中性点）
   - 正值（绿色）：向右延伸，表示喜欢
   - 负值（红色）：向左延伸，表示讨厌
   - 图标：😊（喜欢）/ 😞（讨厌）

## AI 提示词更新

### 关联度说明
```
为每个兴趣维度指定准确的 weight 值（0.1-1.0，只能是正值）
* 1.0 = 强关联：行为直接体现该维度
* 0.7 = 中等关联：行为部分体现该维度
* 0.4 = 弱关联：行为间接涉及该维度
```

### 强度说明
```
为每个兴趣维度指定准确的 intensity 值（-1.0 到 1.0）
* +1.0 = 非常喜欢：孩子表现出强烈的兴趣和积极情绪
* +0.5 = 喜欢：孩子表现出明显的兴趣
* 0.0 = 中性：孩子没有明显的喜欢或讨厌
* -0.5 = 不喜欢：孩子表现出抗拒或回避
* -1.0 = 非常讨厌：孩子表现出强烈的负面情绪
```

## 使用场景示例

### 场景1：孩子兴奋地玩积木
```json
{
  "behavior": "兴奋地玩积木，不愿停止",
  "matches": [
    {
      "dimension": "Construction",
      "weight": 1.0,
      "intensity": 1.0,
      "reasoning": "直接进行建构活动（关联度高），表现出强烈兴趣和积极情绪（强度为正）"
    },
    {
      "dimension": "Visual",
      "weight": 0.7,
      "intensity": 0.8,
      "reasoning": "需要视觉观察和空间感知（关联度中等），孩子专注观察（强度为正）"
    }
  ]
}
```

### 场景2：孩子被动接受触觉刺激
```json
{
  "behavior": "被动接受触摸，没有明显反应",
  "matches": [
    {
      "dimension": "Tactile",
      "weight": 1.0,
      "intensity": 0.0,
      "reasoning": "直接涉及触觉刺激（关联度高），但孩子没有明显的喜欢或讨厌（强度中性）"
    }
  ]
}
```

### 场景3：孩子抗拒噪音
```json
{
  "behavior": "听到突然的响声就捂耳朵",
  "matches": [
    {
      "dimension": "Auditory",
      "weight": 1.0,
      "intensity": -0.8,
      "reasoning": "直接涉及听觉刺激（关联度高），表现出明显的抗拒和回避（强度为负）"
    }
  ]
}
```

## 技术实现

### 文件修改清单
1. `frontend/src/types/index.ts` - 添加 intensity 字段
2. `frontend/src/services/qwenSchemas.ts` - 更新所有 Schema
3. `frontend/src/services/qwenService.ts` - 更新提示词
4. `frontend/src/App.tsx` - 更新工具调用处理和 UI 显示

### 兼容性
- 旧数据自动兼容：如果 intensity 字段不存在，默认为 0.5（正向喜欢）
- 所有新记录都会包含 intensity 字段

## 测试建议

1. **测试正向强度**
   - 输入："孩子正在兴奋地玩积木"
   - 预期：Construction 维度 weight=1.0, intensity=1.0

2. **测试负向强度**
   - 输入："孩子听到响声就捂耳朵"
   - 预期：Auditory 维度 weight=1.0, intensity=-0.8

3. **测试中性强度**
   - 输入："孩子被动地接受触摸"
   - 预期：Tactile 维度 weight=1.0, intensity=0.0

4. **测试多维度不同强度**
   - 输入："孩子喜欢看旋转的东西，但不喜欢别人碰他"
   - 预期：Visual 维度 intensity>0, Tactile 维度 intensity<0
